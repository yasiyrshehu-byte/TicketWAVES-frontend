# TicketWAVES Frontend

Before deployment, edit config.js and replace API_BASE_URL with your deployed backend API, e.g. https://api.example.com/api.

Do not put Paystack secret keys in the frontend.

This frontend expects backend routes for auth, events, payment, tickets, transfers, event seats, users and support. It does not fake successful payments or ticket fulfillment.
