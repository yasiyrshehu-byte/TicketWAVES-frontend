// TICKETWAVES - My Tickets
const TICKET_API="https://ticketwaves-backend-3.onrender.com/api/tickets";const token=localStorage.getItem("token")||localStorage.getItem("ticketwaves_token");
const esc=v=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
async function loadMyTickets(){if(!token){location.href="login.html?next=my-tickets.html";return}const box=document.getElementById("ticketsContainer");if(box)box.innerHTML='<div class="loading">Loading your tickets…</div>';try{const r=await fetch(TICKET_API+"/me",{headers:{Authorization:`Bearer ${token}`}});const d=await r.json();if(!r.ok)throw new Error(d.message||"Unable to load tickets");displayTickets(d.tickets||[]);}catch(e){if(box)box.innerHTML=`<div class="tickets-error"><h3>Unable to load tickets</h3><p>${esc(e.message)}</p><button onclick="loadMyTickets()">Try Again</button></div>`;}}
function displayTickets(tickets){const box=document.getElementById("ticketsContainer");if(!box)return;if(!tickets.length){box.innerHTML='<div class="no-tickets"><h3>No tickets yet</h3><p>Tickets you purchase will appear here.</p></div>';return}box.innerHTML=tickets.map(t=>{const e=t.eventId||{};const id=t._id||t.id;return `<article class="ticket-card"><img src="${esc(t.eventImage||e.image||"images/placeholder.jpg")}" onerror="this.src='images/placeholder.jpg'"><div><h3>${esc(t.eventTitle||e.title||"Event")}</h3><p>${esc(t.eventVenue||e.venue||"")}${e.city?" · "+esc(e.city):""}</p><p>${esc(t.eventDate||e.date||"")} · ${esc(t.eventTime||e.time||"")}</p><p>Section ${esc(t.section||"Not assigned")} · Row ${esc(t.row||"Not assigned")} · Seat ${esc(t.seat||"Not assigned")}</p><p>Ticket #${esc(t.ticketCode||t.ticketNumber||id)}</p><button onclick="location.href='ticket.html?ticket=${encodeURIComponent(id)}'">View Ticket</button></div></article>`}).join("");}
document.addEventListener("DOMContentLoaded",loadMyTickets);
