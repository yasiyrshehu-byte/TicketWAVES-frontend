const KEY='ticketwaves_api_url';function token(){return localStorage.tw_token||''}function base(){return(localStorage.getItem(KEY)||'').replace(/\/$/,'')}function saveApi(){const v=prompt('Render backend URL (do not add /api):',base());if(v&&/^https?:\/\//.test(v)){localStorage.setItem(KEY,v.replace(/\/$/,''));location.reload()}}function api(path,opt={}){const b=base();if(!b)throw Error('Set your Render backend URL first.');const h={Authorization:'Bearer '+token(),...(opt.headers||{})};if(!(opt.body instanceof FormData))h['Content-Type']='application/json';return fetch(b+path,{...opt,headers:h}).then(async r=>{const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||`Request failed (${r.status})`);return d})}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function money(n,c='NGN'){return new Intl.NumberFormat(undefined,{style:'currency',currency:c}).format(Number(n||0))}function setup(){const el=document.getElementById('api');if(el)el.innerHTML=`<div class="setup"><b>Backend connection</b><p class="muted">Your admin site can be on GitHub Pages while the API is on Render.</p><input id="apiUrl" placeholder="https://your-service.onrender.com" value="${esc(base())}"><button class="btn primary" onclick="saveApi()">Save API URL</button></div>`}document.addEventListener('DOMContentLoaded',setup);
