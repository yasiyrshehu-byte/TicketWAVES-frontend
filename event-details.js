(async()=>{const id=new URLSearchParams(location.search).get('id');const b=document.getElementById('details');if(!id){b.textContent='Event not found.';return}try{const d=await api('/events/'+id),e=d.event||d.data||d;b.innerHTML=`<img class="detail-img" src="${esc(e.image||'images/placeholder.jpg')}" onerror="this.style.display='none'"><div><span class="tag">${esc(e.category||'Event')}</span><h1>${esc(e.title)}</h1><h2>${esc(e.artist||'')}</h2><p>${esc(e.description||'')}</p><p><b>${esc(e.venue)}</b><br>${esc(e.city)}, ${esc(e.country)}<br>${esc(e.date)} · ${esc(e.time)}</p><h2>${money(e.price,e.currency)}</h2><button class="primary" onclick="buy(${e.id})">Buy tickets</button></div>`}catch(e){b.innerHTML=`<div class="empty error">${esc(e.message)}</div>`}})();function buy(id){if(!getToken()){location.href='login.html?redirect='+encodeURIComponent(location.href);return}location.href='checkout.html?event='+id}
