loginForm?.addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({email:email.value.trim(),password:password.value})});localStorage.setItem('token',d.token||d.accessToken);if(d.user||d.data?.user)localStorage.setItem('user',JSON.stringify(d.user||d.data.user));location.href=new URLSearchParams(location.search).get('redirect')||'index.html'}catch(x){message.textContent=x.message}});
registerForm?.addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('/auth/register',{method:'POST',body:JSON.stringify({firstName:firstName.value.trim(),lastName:lastName.value.trim(),email:email.value.trim(),phone:phone.value.trim(),country:country.value.trim(),password:password.value})});if(d.token){localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user||d.data?.user||{}));location.href='index.html'}else location.href='login.html'}catch(x){message.textContent=x.message}});
