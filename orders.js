(async()=>{if(!requireAuth())return;try{const u=getUser(),d=await api('/tickets/user/'+u.id),ts=d.tickets||d.data||[];orders.innerHTML=ts.length?ts.map(t=>`<article class="event-card"><div class="event-body"><span class="tag">${esc(t.status||'Active')}</span><h3>${esc(t.eventTitle||t.title||'Ticket')}</h3><p>${esc(t.date||t.eventDate||'')} · ${esc(t.venue||t.eventVenue||'')}</p><p>Section ${esc(t.section||'—')} · Row ${esc(t.row||'—')} · Seat ${esc(t.seat||'—')}</p><p>Ticket: ${esc(t.ticketNumber||t.id)}</p><a class="button" href="ticket-details.html?ticket=${t.id}">View ticket</a></div></article>`).join(''):'<div class="empty">No tickets yet. <a href="events.html">Browse events</a></div>'}catch(e){orders.innerHTML=`<div class="empty error">${esc(e.message)}</div>`}})();
